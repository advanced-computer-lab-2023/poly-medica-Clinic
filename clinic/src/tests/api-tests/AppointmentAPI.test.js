
// Sample test file for API testing

/*
import request from 'supertest';
import app from '../../../app.js';

import { 
	connectDBTest,
	disconnectDBTest
} from '../../utils/testing-utils.js';


import { describe, beforeEach, afterEach, expect, it } from '@jest/globals';

describe('GET /things', () => {
    
	beforeEach(async () => {
		await connectDBTest();
	});

	it('test name', async () => {
		const res = request(app).get('/things');
		expect(res).toBe(200);
	});

	afterEach(async () => {
		await disconnectDBTest();
	});
});


*/